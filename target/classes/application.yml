mybatis-plus:
#  config-location: classpath:mybatis-config.xml
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: com.ch.lesson.domain
server:
  port: 8081
  servlet:
    context-path: /lesson